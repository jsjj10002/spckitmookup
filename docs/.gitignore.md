# .gitignore 가이드

## 📋 무시되는 항목 설명

### Node.js / Frontend
- `node_modules/` - npm 패키지
- `dist/`, `dist-ssr/` - 빌드 출력물
- `*.local` - 로컬 환경 변수 파일

### Python / Backend
- `__pycache__/`, `*.pyc` - Python 컴파일 캐시
- `.venv/`, `backend/.venv/` - Python 가상 환경
- `.pytest_cache/` - 테스트 캐시
- `*.egg-info/` - Python 패키지 메타데이터

### 데이터베이스
- `backend/chroma_db/` - ChromaDB 벡터 저장소
  - **이유**: 로컬에서 생성된 벡터 DB는 용량이 크고, 프로덕션에서 재생성됨
  - 초기화: `backend/run_init.bat` 또는 `python backend/scripts/init_database.py`

### 환경 변수
- `.env`, `.env.local` - API 키 등 민감 정보
- `backend/.env` - 백엔드 환경 변수

### 백업
- `backup/` - 이전 버전 프론트엔드
  - `old_react_frontend/`
  - `frontbackup/`

### 기타
- 에디터 설정 (`.vscode/`, `.idea/`)
- OS 파일 (`.DS_Store`, `Thumbs.db`)
- 임시 파일 (`*.tmp`, `*.swp`)

## ✅ 포함되는 중요 파일

```
✓ README.md           # 프로젝트 문서
✓ CHANGELOG.md        # 변경 이력
✓ DEPLOYMENT.md       # 배포 가이드
✓ package.json        # Node.js 의존성
✓ pyproject.toml      # Python 프로젝트 설정
✓ frontend/           # 소스 코드
✓ backend/            # 소스 코드
✓ assets/             # 에셋
✓ backend/data/       # SQL 데이터 (무시됨, 필요시 LFS)
```

## 🔒 보안 주의사항

**절대 커밋하지 말 것:**
- API 키 (`.env*` 파일)
- 비밀번호
- 개인 정보
- 프로덕션 데이터베이스 정보

## 📦 Git LFS 사용 권장

대용량 파일은 Git LFS 사용을 권장합니다:

```bash
git lfs track "backend/data/*.sql"
git lfs track "*.pdf"
```

## 🔄 .gitignore 업데이트 후

```bash
# 이미 추적 중인 파일 제거
git rm -r --cached .
git add .
git commit -m "chore: update .gitignore"
```

